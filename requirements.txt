# Web Framework & API
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.9.2
python-multipart          # Datei-Uploads in FastAPI

# Datenbank
sqlalchemy==2.0.36
psycopg2-binary==2.9.10   # Postgres-Treiber (Supabase)
pgvector==0.3.6           # vector(384)-Spalten + Cosine-Suche in SQL
supabase==2.10.0          # Supabase-Client (Auth-Helfer)
PyJWT[crypto]==2.10.1     # Supabase-JWT-Verifikation (HS256 + JWKS)

# Ähnlichkeitssuche (Embeddings)
# Hinweis: sentence-transformers zieht torch + transformers als Abhängigkeit.
sentence-transformers==3.3.1

# Import & Export
pandas==2.2.3             # CSV/Excel-Import
numpy==1.26.4             # Cosine-Fallback (SQLite)
openpyxl                  # Excel-Dateien (.xlsx) lesen
reportlab==4.2.5          # Angebots-PDF

# Konfiguration
python-dotenv==1.0.1      # .env-Dateien für lokale Konfiguration
